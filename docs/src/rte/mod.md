# rte/mod.rs — RTE 모듈 인덱스

- 경로: `src/rte/mod.rs`
- 계층: RTE / 상위 모듈

## 목적
- 런타임 환경(RTE)의 DTO, 채널 초기화, 카메라 버퍼 유틸을 모아 상위 계층이 단일 모듈에서 가져올 수 있도록 합니다.
- BSW/ASW 태스크와 엔트리포인트가 공통 타입을 공유할 때 경로를 명확히 합니다.

## 하위 모듈
- `rte_dto`: VFB DTO 구조체 정의.
- `rte_main`: 브로드캐스트 채널 초기화 및 공유 핸들 생성.
- `lib`: 카메라 버퍼 등 유틸리티.
